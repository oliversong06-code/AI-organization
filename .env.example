# Copy to .env for local dev. Never commit the real .env file.
# SQLite path is resolved relative to the current working directory
# (verified empirically: NOT relative to prisma/schema.prisma).
DATABASE_URL="file:./prisma/dev.db"
